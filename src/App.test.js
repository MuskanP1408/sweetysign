// // Home.test.js (or your test file)
// import { render, screen } from '@testing-library/react';
// import Home from './components/Home';  // Ensure path is correct

// test('renders Hello World text', () => {
//   render(<Home />);
//   const headingElement = screen.getByText(/hello, world!/i);  // Check for "Hello, World!" text
//   expect(headingElement).toBeInTheDocument();
// });

// test('renders Learn React link', () => {
//   render(<Home />);
//   const linkElement = screen.getByText(/learn react/i);  // Check for "Learn React" link
//   expect(linkElement).toBeInTheDocument();
// });
